import React from 'react';

import tableUsers from './Table.module.css'

const Table = () => {
    return (
        <div className={tableUsers.table_users}>
            <table>

            </table>
        </div>
    );
};

export default Table;
